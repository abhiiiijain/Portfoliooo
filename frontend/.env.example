# Frontend — deploy on Vercel (project root: frontend)
PORT=4041

# Backend API on Render (no trailing slash) — used by browser and SSR
NEXT_PUBLIC_API_URL=http://localhost:4042

# Production (Render backend):
# NEXT_PUBLIC_API_URL=https://portfoliooo-xr2q.onrender.com
