# Frontend (Vercel project root: frontend)
PORT=4041

# Backend API URL (no trailing slash) — used by browser and SSR
NEXT_PUBLIC_API_URL=http://localhost:4042

# Production example:
# NEXT_PUBLIC_API_URL=https://your-backend.vercel.app
